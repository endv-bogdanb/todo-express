import assert from "node:assert";
import { describe, it } from "node:test";

describe("hell world", () => {
  it("should do stuff", () => {
    assert.equal(true, true);
    assert.ok(true);
  });
});
